{
  "name": "dotfiles-pi-extensions",
  "private": true,
  "description": "Deps for the code-intel and lsp pi extensions. pi loads extensions via jiti, which resolves node_modules from a parent directory, so one install here covers both.",
  "dependencies": {
    "vscode-jsonrpc": "^8.2.1"
  }
}
