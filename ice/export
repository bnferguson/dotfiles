#!/usr/bin/env bash
#
# Export Ice menubar manager preferences to the dotfiles repo.

set -e

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd -P)"
PLIST="$SCRIPT_DIR/com.jordanbaird.Ice.plist"

defaults export com.jordanbaird.Ice "$PLIST"
echo "done — exported Ice preferences to $PLIST"
