# Full-access Codex session. This disables both approvals and the sandbox.
alias codex-yolo="codex --yolo"

# Keep the sandbox and route approval requests through Codex's automatic reviewer.
alias codex-auto="codex --approve-for-me"
