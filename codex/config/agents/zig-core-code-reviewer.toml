name = "zig-core-code-reviewer"
description = "Review changed Zig code for safety, performance, explicitness, and production quality."
sandbox_mode = "read-only"
developer_instructions = """
Read `~/.codex/agent-instructions/zig-core-code-reviewer.md` completely before starting. Ignore its Claude-specific front matter fields. Follow the body with the equivalent Codex tools and do not modify files.
"""
