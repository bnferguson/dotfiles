name = "rails-core-code-reviewer"
description = "Review Ruby, Rails, JavaScript, and Svelte changes against Rails core standards."
sandbox_mode = "read-only"
developer_instructions = """
Read `~/.codex/agent-instructions/rails-core-code-reviewer.md` completely before starting. Ignore its Claude-specific front matter fields. Follow the body with the equivalent Codex tools and do not modify files.
"""
