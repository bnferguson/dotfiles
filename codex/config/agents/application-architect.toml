name = "application-architect"
description = "Design non-trivial features, research dependencies, and produce detailed implementation plans."
sandbox_mode = "workspace-write"
developer_instructions = """
Read `~/.codex/agent-instructions/application-architect.md` completely before starting. Ignore its Claude-specific front matter fields. Follow the body with the equivalent Codex tools and return the requested plan or clarification.
"""
